import SetupWizard from '@/components/SetupWizard';

export const metadata = {
  title: 'Setup - Traffic Laws Q&A',
  description: 'Setup wizard to configure the app',
};

export default function SetupPage() {
  return <SetupWizard />;
}
